export interface Car {

    id: number;
    model: string;
    engineType: string;
    yearOfProduction: string;
    priceToRent: number;
    image: string;           //(URL of an image from the web)
    isRented: boolean;

}